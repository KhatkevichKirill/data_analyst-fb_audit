# Data Analyst Knowledge Base

Knowledge pages for the `read_wiki` tool. Defaults match the [fb_audit](https://github.com/KhatkevichKirill/fb_audit) PostgreSQL schema.

## Start here

| Page | When to read |
|---|---|
| `onboarding` | First session — mental model and setup |
| `main_overview` | KPI / trend / performance questions |
| `concepts` | IDs, timezones, metrics, date windows |
| `schema` | Table catalog and script mapping |
| `reference` | Copy-paste SQL patterns |

## Schema deep-dives

| Page | Contents |
|---|---|
| `schema_insights` | `insights`, `insights_log`, `intraday_insights`, `actions` |
| `schema_entities` | `property_*`, `deleted_objects` |
| `schema_breakdowns` | Extension point — not in core fb_audit |

## Recurring analytics

| Page | Contents |
|---|---|
| `weekly_playbook` | Standard weekly checks |

Override the directory with `KNOWLEDGE_DIR` in `.env` if you maintain docs elsewhere.
